#include <stdio.h>
/**
 * main - Entry point.
 * @argc: int.
 * @argv: string.
 * Return: 0 is (success).
*/

int main(int argc, char const *argv[])
{
(void)argv;
printf("%i\n", argc - 1);
return (0);
}
