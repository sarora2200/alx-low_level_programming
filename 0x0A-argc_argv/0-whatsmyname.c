#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: is the count of the input arguments.
 * @argv: is an input string of size argc.
 * Return: 0
*/

int main(int argc, char const *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
