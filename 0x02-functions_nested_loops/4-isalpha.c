#include "main.h"

/**
 * _isalpha - function that checks for alphabetic character.
 * @c: is the char to be checked.
 * Return: 1 if the char is a letter, otherwisw give 0.
*/

int _isalpha(int c)
{  if
(c >= 'a' && c <= 'z')
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
