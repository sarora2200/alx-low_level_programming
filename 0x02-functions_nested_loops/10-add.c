#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @a: ia first input.
 * @n: is secound input
 * Return: sum of a & n.
*/
int add(int a, int n)
{ int e = a + n;
	return (e);
}
