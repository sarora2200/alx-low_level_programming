#include "main.h"

/**
 * mul - function that multiplies two integers.
 *
 * @a: is input number.
 *
 * @b: is input number.
 *
 * Return: e is (success).
*/
int mul(int a, int b)
{
int e;
e = a * b;
return (e);
}
