#include "main.h"

/**
 * mul - function that multiplies two integers.
 *
 * @a: is input number.
 *
 * @b: is input number.
 *
 * Return: 0 is (success).
*/
int mul(int a, int b)
{
int e;
e = a * b;
_putchar(e);
_putchar('\n');
return (0);
}
