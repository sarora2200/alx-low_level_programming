#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print alphabet in reverse
 *
 * Return: 0 (success)
*/

int main(void)
{ char ch = 'z'
for (ch = 122; ch >= 'a'; ch--)
{
putchar(ch)
}
putchar('\n')
return (0);
}
